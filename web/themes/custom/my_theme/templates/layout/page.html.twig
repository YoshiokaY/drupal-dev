{#
/**
 * @file
 * Theme implementation to display a single page.
 */
#}
<div class="layout_container">
  <header class="header" role="banner">
    <div class="header_inner">
      <a href="{{ path('<front>') }}" class="header_logo">
        <span>Sample Corp.</span>
      </a>
      <nav class="header_nav" aria-label="{{ 'Main navigation'|t }}">
        {{ page.primary_menu }}
      </nav>
    </div>
  </header>

  {% if page.secondary_menu %}
    <nav class="secondary_nav" role="navigation">
      {{ page.secondary_menu }}
    </nav>
  {% endif %}

  {% if page.breadcrumb %}
    <div class="breadcrumb_wrapper">
      {{ page.breadcrumb }}
    </div>
  {% endif %}

  {% if page.highlighted %}
    <div class="highlighted">
      {{ page.highlighted }}
    </div>
  {% endif %}

  {% if page.help %}
    <div class="help">
      {{ page.help }}
    </div>
  {% endif %}

  <main class="main" role="main">
    <a id="main-content" tabindex="-1"></a>
    {{ page.content }}
  </main>

  <footer class="footer" role="contentinfo">
    <div class="footer_inner">
      <div class="footer_content">
        <div class="footer_brand">
          <p class="footer_logo">Sample Corp.</p>
          <p class="footer_tagline">{{ 'ビジネスを成功に導くパートナー'|t }}</p>
        </div>
        <nav class="footer_nav" aria-label="{{ 'Footer navigation'|t }}">
          {{ page.footer }}
        </nav>
      </div>
      <p class="footer_copyright">
        <small>&copy; {{ 'now'|date('Y') }} Sample Corp. All rights reserved.</small>
      </p>
    </div>
  </footer>
</div>
